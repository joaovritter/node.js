<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% const message = 'Hello world'%>

    <h1>Welcome to the Home Page</h1>
    <h2><%= message %></h2>
    <p>This is a beaultiful EJS template.</p>

    <div class="contentt">
        <div class="conteiner">
            <% if (users.length > 0) { %> <% users.forEach( user => { %>
            <h3 class="title"> 
                <%= user.firstName %> <% user.lastName %> 
            </h3>

            <p> <% user.email %>  </p>

            <% }) %> <% } else { %>
            <p>No users found.</p>
            <% } %>
        </p>
        </div>
    </div>


</body> 
</html>